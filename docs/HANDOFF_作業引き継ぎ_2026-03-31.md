# 作業引き継ぎメモ（2026-03-31）

**リポジトリ**: [github.com/minougun/maguromaru-note](https://github.com/minougun/maguromaru-note)  
**ローカル例**: `/mnt/c/Users/minou/maguromaru-note`  
**本番**: [maguromaru-note.vercel.app](https://maguromaru-note.vercel.app)（`master` push で Vercel 自動デプロイ）

---

## 本セッションで確定しておきたいこと

### 1. 大とろ・中とろの表示色（重要）

- **意図**: ユーザー提供スクショのトーンに合わせた色。DB が古くてもアプリ表示は揃える。
- **値**（`src/lib/domain/part-brand-colors.ts` で単一ソース）:
  - 大とろ `otoro`: **`#eb7e7c`**
  - 中とろ `chutoro`: **`#d66078`**
- **反映経路**:
  - `listMasterDataPartial`（`src/lib/services/app-service.ts`）が `parts` を返す直前に **`applyOtoroChutoroDisplayColors`** を実行 → スナップショット経由の図鑑・記録・マップラベル等に反映。
  - `seed.ts` も同定数を参照（モック・クイズ用シードと一致）。
- **Supabase**: `supabase/migrations/202603312200_set_otoro_chutoro_reference_swatch_colors.sql` で `parts` 行を冪等更新。**未適用でも UI は上書きで合う**が、DB を正としたい場合は適用推奨。
- **注意**: 過去に色の入れ替え・ティント・正規化を試し **いったん全 revert** した後、この方針に落ち着いている。**マップの塗り**は `zukan-tuna-map-reveal.webp` の焼き付け色のまま（`mix-blend` ティントは入れていない）。

### 2. マイグレーション履歴（色まわり）

- **`202603312100_reset_otoro_chutoro_senaka_baseline_colors.sql`**: 以前の「既定色へ戻す」用（`#ff6b6b` / `#e85555` / senaka `#d35050`）。**その後** `312200` で大とろ・中とろを参考色へ再度更新。
- 新規クローンで `312100` → `312200` の順に流れると、最終的に大とろ・中とろは `312200` の値になる。
- リモートで **削除済みの古い色用 migration** を一度でも適用しているプロジェクトは、Supabase CLI の履歴とローカルファイルの整合に注意（必要なら `migration repair` 等）。

### 3. 図鑑マップ `TunaMap.tsx`（座標・形状）

- **腹・大とろ**: 頭側／尾側ブロックの境は **`562,472` 共有**、上縁は **三次ベジェ**で滑らかに接続（選択時の黒枠の V 字解消目的）。
- **中とろ（腹）**: 下端を銀帯付近まで **下方向に拡張**済み（余白削減）。
- **中とろ（背）**: 上縁を **約 20px 上**、下縁を **約 20px 下**に拡張済み。**背中の隙間を埋める第4サブパス**はユーザー要望で **revert**（尾ブロックとの帯は未塗りのまま）。
- **大とろ（腹）リーダー**: 尾側ラベルの `lineTo` は **`(696, 486)`** 付近（へりの V に合わせた微調整履歴あり）。
- 詳細の時系列は `docs/HANDOFF_図鑑部位マップ_経緯_2026-03-30.md` も参照。

### 4. ホーム・まぐろ丸 Bot（A 案）

- **プレースホルダ**: `aiStoreBlurb` が `null` でも **のれん直下**に薄いカードを表示（初見向け説明）。
- **実装**: `src/components/screens/HomeScreen.tsx`、`.ai-store-blurb-card--placeholder` 等は `globals.css`。
- **データ**: `store_ai_blurbs`、Cron は Hobby 制約により **GitHub Actions**（`.github/workflows/store-ai-blurb-cron.yml`）で `*/30` 等。Vercel `vercel.json` の crons は **削除済み**の経緯あり。

### 5. ドキュメント・アセット

- **SVG モックアップ**: `docs/home-bot-ui-mockup.xml`（過去に XML 破損があり **UTF-8 で書き直し**／現在は英語・ローマ字ラベル中心の安全版）。
- **AGENTS.md / ルール**: ワークスペースの `/mnt/c/Users/minou/AGENTS.md` に `maguromaru-note` の push・Vercel 方針あり。

---

## 主要ファイル一覧（本日周辺）

| 内容 | パス |
|------|------|
| 大とろ・中とろの表示色定数＋上書き関数 | `src/lib/domain/part-brand-colors.ts` |
| マスタ `parts` 取得＋色適用 | `src/lib/services/app-service.ts`（`listMasterDataPartial`） |
| モック用シード | `src/lib/domain/seed.ts` |
| 図鑑マップ | `src/components/TunaMap.tsx` |
| 図鑑画面 | `src/components/screens/ZukanScreen.tsx` |
| ホーム（Bot カード／プレースホルダ） | `src/components/screens/HomeScreen.tsx` |
| 部位色の DB 初期行 | `supabase/migrations/202603280001_init.sql` |
| 参考色の DB 更新 | `supabase/migrations/202603312200_set_otoro_chutoro_reference_swatch_colors.sql` |

---

## 次に触るかもしれない作業

- **色の再調整**: `part-brand-colors.ts` の 2 定数と、`312200` / `init.sql` / `seed.ts` を同期。
- **マップとイラストのズレ**: `MAP_REGIONS` の path のみ調整（`scripts/build-map-regions-from-reveal.py` 参照コメントあり）。
- **Supabase**: `312200` 未適用環境の整理、本番 `parts` の手動確認（SQL Editor）。

---

## デプロイ・確認

1. `git push origin master` 後、Vercel でデプロイ成功を確認。
2. 本番で **ハードリロード**（`Cache-Control: no-store` でもクライアントのスナップショットは最大数十秒キャッシュあり → `AppSnapshotProvider` の stale 設定参照）。
3. 図鑑の色は **デプロイだけで** `part-brand-colors` 経由で変わる想定。

---

## 過去メモとの関係

- 2026-03-30 版の全体像: `docs/HANDOFF_作業引き継ぎ_2026-03-30.md`
- 部位マップ経緯: `docs/HANDOFF_図鑑部位マップ_経緯_2026-03-30.md`
